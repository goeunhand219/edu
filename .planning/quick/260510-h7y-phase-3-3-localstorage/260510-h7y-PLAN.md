---
phase: quick-260510-h7y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/emotions.js
  - src/lib/storage.js
  - src/components/EmotionSticker.jsx
  - src/components/EmotionStickerGrid.jsx
  - src/components/RecordTextField.jsx
  - src/components/RecordAmountField.jsx
  - src/components/BackButton.jsx
  - src/components/HelperText.jsx
  - src/screens/RecordingScreen.jsx
  - src/App.jsx
  - src/components/MobileFrame.jsx
  - vite.config.js
  - package.json
  - .planning/quick/260510-h7y-phase-3-3-localstorage/DEPLOY.md
autonomous: true
requirements: [REC-01, REC-02, REC-03, REC-04, REC-05, PLAT-03, PLAT-04]

must_haves:
  truths:
    - "사용자가 화면 3에서 품목·금액·감정을 입력하고 '마법서에 적기'를 누르면 메인 화면으로 돌아간다"
    - "금액 입력 시 숫자만 허용되고 표시는 자동으로 콤마(3,000) 포맷으로 보인다"
    - "5종 감정 스티커(😍🤔😅😢✨) 중 하나를 클릭하면 골드 테두리·1.1배 확대로 강조되고 나머지는 opacity 0.4로 흐려진다"
    - "품목·금액·감정 셋 중 하나라도 비어 있으면 '마법서에 적기' 버튼이 비활성화되고 어떤 항목이 비었는지 안내문이 보인다"
    - "기록 저장 시 record={item, amount, emotion, timestamp} 가 localStorage 'records' 배열에 push 되고 새로고침 후에도 유지된다"
    - "좌상단 '← 돌아가기' 버튼으로 입력 폐기 후 메인 화면으로 복귀할 수 있다"
    - "npm run build 가 에러 없이 완료되고 dist/ 폴더에 index.html과 assets 가 생성된다"
    - "프로덕션 빌드를 npx vite preview 로 띄우면 화면 1→2→3 전체 흐름이 작동한다"
    - "DEPLOY.md 가이드를 따라 사용자가 1~3분 안에 시연용 공개 URL 을 얻을 수 있다"
  artifacts:
    - path: "src/data/emotions.js"
      provides: "5종 감정 메타데이터 (key·emoji·label·helper)"
      contains: "export const EMOTIONS"
    - path: "src/lib/storage.js"
      provides: "localStorage records 추상화 (loadRecords / appendRecord)"
      exports: ["loadRecords", "appendRecord"]
    - path: "src/components/EmotionSticker.jsx"
      provides: "단일 감정 스티커 (선택/흐림/포커스 상태)"
    - path: "src/components/EmotionStickerGrid.jsx"
      provides: "5종 감정 스티커 라디오그룹"
    - path: "src/components/RecordTextField.jsx"
      provides: "품목 텍스트 입력 (라벨 + 골드 테두리 input)"
    - path: "src/components/RecordAmountField.jsx"
      provides: "금액 숫자 입력 (콤마 포맷 + 원 단위 표시)"
    - path: "src/components/BackButton.jsx"
      provides: "← 돌아가기 좌상단 버튼 (≥48px)"
    - path: "src/components/HelperText.jsx"
      provides: "비어있는 항목 안내문 라벨"
    - path: "src/screens/RecordingScreen.jsx"
      provides: "화면 3 합성 (헤더 + 두 입력 + 5스티커 + CTA + 돌아가기)"
      min_lines: 70
    - path: "src/App.jsx"
      provides: "recording 화면을 RecordingScreen 으로 교체 + onSave 핸들러"
    - path: "vite.config.js"
      provides: "base: './' 추가 (Netlify drop · 정적 호스팅 호환)"
      contains: "base: './'"
    - path: ".planning/quick/260510-h7y-phase-3-3-localstorage/DEPLOY.md"
      provides: "Netlify drop / Vercel CLI 두 가지 배포 가이드 + URL 기록 안내"
  key_links:
    - from: "src/screens/RecordingScreen.jsx"
      to: "src/lib/storage.js (appendRecord)"
      via: "마법서에 적기 클릭 핸들러"
      pattern: "appendRecord"
    - from: "src/lib/storage.js"
      to: "localStorage 'records' 키"
      via: "JSON.stringify / JSON.parse"
      pattern: "localStorage\\.(getItem|setItem)\\(['\"]records['\"]"
    - from: "src/App.jsx"
      to: "src/screens/RecordingScreen.jsx"
      via: "screen === 'recording' 분기"
      pattern: "RecordingScreen"
    - from: "src/screens/RecordingScreen.jsx"
      to: "src/data/emotions.js (EMOTIONS)"
      via: "EmotionStickerGrid 렌더 매핑"
      pattern: "EMOTIONS\\.map|import.*EMOTIONS"
---

<objective>
Phase 3 화면 3 (기록) 완성 + localStorage 영속화 + 시연 URL 확보. 화면 1→2→3 전체 흐름이 작동하고 새로고침 후에도 데이터가 유지되며, 사용자가 가이드에 따라 1~3분 안에 공개 URL 을 얻을 수 있는 상태까지 가져간다.

Purpose: 앱의 차별점인 "감정 태깅 → 기록 → 자기 성찰" 핵심 플로우의 마지막 조각. 화면 3가 완성되고 배포 URL 이 생기는 순간 v1 시연이 가능해지고 포트폴리오로 사용할 수 있다.
Output: RecordingScreen + 5종 감정 스티커 컴포넌트 + storage 추상화 + 프로덕션 dist + DEPLOY.md 가이드.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/01-foundation-onboarding/01-UI-SPEC.md
@CLAUDE.md
@경제마법사학교_기능설명서.md
@src/App.jsx
@src/data/attributes.js
@src/data/dummyData.js
@src/screens/DashboardScreen.jsx
@src/screens/PlaceholderScreen.jsx
@src/components/MobileFrame.jsx
@src/components/SparkleBackground.jsx
@src/components/ScreenTitle.jsx
@src/components/PrimaryButton.jsx
@src/components/MagicCoin.jsx
@tailwind.config.js
@vite.config.js
@package.json

<interfaces>
<!-- 재사용할 기존 export 들. 파일을 다시 열지 말고 이 표를 그대로 사용. -->

From src/data/attributes.js:
```js
export const ATTRIBUTES; // [{key, emoji, label, color, title, subtitle}, ...]
```

From src/data/dummyData.js:
```js
export const user;     // {name, level, coins}
export const today;    // {lesson, weeklyMission}
```

From src/components/MobileFrame.jsx:
```jsx
export default function MobileFrame({ children })
// 375x667 카드, 자식은 z-10. min-h-[667px] 컨테이너 안.
```

From src/components/SparkleBackground.jsx:
```jsx
export default function SparkleBackground()
```

From src/components/ScreenTitle.jsx:
```jsx
export default function ScreenTitle({ children })  // gold 24px h1, center.
```

From src/components/PrimaryButton.jsx:
```jsx
export default function PrimaryButton({ children, disabled = false, onClick })
// aria-disabled 패턴 (HTML disabled 미사용). 비활성: bg-white/20 text-white/40.
// 활성: bg-cta-magenta. min-h-[48px], rounded-button, full width.
```

From src/components/MagicCoin.jsx (감정 스티커 패턴 참고용):
```jsx
// role="radio" + aria-checked. isSelected → shadow-coin-selected scale-110.
// isDimmed → opacity-40. focus-visible:shadow-focus-ring.
// 이 패턴을 EmotionSticker 에서 그대로 차용 (동전 → 스티커 박스로만 변경).
```

From src/screens/DashboardScreen.jsx:
```jsx
export default function DashboardScreen({ selectedKey, onRecord, onBackToOnboarding })
// onRecord 가 setScreen('recording') 으로 라우팅. 그대로 사용.
```

From src/App.jsx (변경 대상):
```jsx
// 현재: screen === 'recording' → <PlaceholderScreen selectedKey={...} />
// 변경: screen === 'recording' → <RecordingScreen onSave={...} onBack={...} />
// onSave = (record) => { appendRecord(record); setScreen('dashboard'); }
// onBack = () => setScreen('dashboard');
```

Tailwind tokens (이미 정의됨, 그대로 사용):
- color: bg-magic-bg, text-magic-gold, text-magic-cream, bg-cta-magenta, ring/border 색상
- shadow: shadow-coin-selected, shadow-cta-hover, shadow-focus-ring
- font-size: text-label / text-body / text-heading / text-display
- radius: rounded-card, rounded-button
- animation: animate-float, animate-coin-spin, animate-sparkle-up, animate-fade-in
</interfaces>

<copy_contract>
<!-- PRD PART 3 그대로. 글자 한 자도 바꾸지 말 것. -->

[헤더]
📖 오늘의 마법서
오늘 너의 모험을 적어볼까?

[입력 라벨]
무엇을 샀어?
얼마였어?
살 때 마음이 어땠어?

[감정 스티커 5종 — emoji + label + key]
😍 꼭 필요했어   → key: need
🤔 잠깐 고민했어 → key: considered
😅 그냥 갖고 싶었어 → key: impulse
😢 사고 나니 후회돼 → key: regret
✨ 정말 최고야!   → key: satisfied

[CTA]
마법서에 적기 ✍️

[비활성 시 안내문 — 비어있는 항목별 1줄. 모두 표시되는 게 아니라 비어있는 것만.]
무엇을 샀는지 알려줘!   ← item 비어있을 때
얼마였는지 알려줘!     ← amount === 0 또는 비어있을 때
마음이 어땠는지 알려줘! ← emotion 비어있을 때

[돌아가기 — 좌상단]
← 돌아가기

[금액 입력 표기]
input 안에는 콤마 포맷된 숫자만 (예: "3,000"). placeholder 비움 (라벨이 위에 따로).
input 우측에 "원" 접미사를 inline 으로 표시 (input 옆 span).
</copy_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 화면 3 (RecordingScreen) 컴포넌트 + 폼 검증 + 감정 스티커 그리드 구현</name>
  <files>
    src/data/emotions.js,
    src/components/EmotionSticker.jsx,
    src/components/EmotionStickerGrid.jsx,
    src/components/RecordTextField.jsx,
    src/components/RecordAmountField.jsx,
    src/components/BackButton.jsx,
    src/components/HelperText.jsx,
    src/screens/RecordingScreen.jsx,
    src/components/MobileFrame.jsx
  </files>
  <action>
화면 3 의 모든 부품과 합성 화면을 만든다. localStorage 저장은 Task 2 에서 결합하므로 여기서는 onSave / onBack 콜백만 props 로 받는다.

**1. src/data/emotions.js — 5종 감정 메타데이터**
PRD PART 3 카피를 글자 그대로. 순서는 PRD 그리드 순서(😍🤔😅 / 😢✨)와 동일하게 유지.
```js
// 5종 감정 메타데이터 — PRD PART 3 그대로.
// emotion 값(key)은 PRD PART 5 record.emotion 명세: need|considered|impulse|regret|satisfied
// helper 는 비활성 시 안내문이 아닌, 향후 화면 4(친구 매핑)·접근성 라벨용 짧은 설명.
export const EMOTIONS = [
  { key: 'need',      emoji: '😍', label: '꼭 필요했어' },
  { key: 'considered',emoji: '🤔', label: '잠깐 고민했어' },
  { key: 'impulse',   emoji: '😅', label: '그냥 갖고 싶었어' },
  { key: 'regret',    emoji: '😢', label: '사고 나니 후회돼' },
  { key: 'satisfied', emoji: '✨', label: '정말 최고야!' },
];
```

**2. src/components/EmotionSticker.jsx — 단일 스티커**
MagicCoin.jsx 의 selected/dimmed/focus 패턴을 그대로 차용하되, 원형 동전이 아니라 카드 박스(rounded-card, 반투명 흰색 배경, 골드 테두리)로 만든다.
```jsx
// EmotionSticker — 단일 감정 스티커. (선택 / 흐림 / 포커스).
// MagicCoin 의 role=radio + aria-checked + shadow-coin-selected/scale-110 패턴을 차용.
// 박스 형태: rounded-card, 반투명 흰색 배경, 1px 골드 테두리. 선택 시 1.1배 확대 + 골드 글로우.
// min-h-[48px] min-w-[48px] (PLAT-02). focus-visible:shadow-focus-ring.
export default function EmotionSticker({ emoji, label, emotionKey, isSelected, isDimmed, onSelect }) {
  return (
    <button
      type="button"
      role="radio"
      aria-checked={isSelected}
      aria-label={`${label} 감정`}
      onClick={() => onSelect(emotionKey)}
      className={[
        'min-h-[88px] min-w-[88px] px-2 py-3',
        'flex flex-col items-center justify-center gap-1',
        'rounded-card bg-white/10 border border-magic-gold/60',
        'text-white cursor-pointer',
        'transition-all duration-300 ease-out',
        'focus-visible:outline-none focus-visible:shadow-focus-ring',
        isSelected ? 'scale-110 shadow-coin-selected' : '',
        isDimmed ? 'opacity-40' : 'opacity-100',
      ].join(' ')}
    >
      <span className="text-[32px] leading-none select-none" aria-hidden="true">{emoji}</span>
      <span className="text-label leading-tight text-center">{label}</span>
    </button>
  );
}
```

**3. src/components/EmotionStickerGrid.jsx — 5종 라디오그룹**
3+2 그리드(첫 줄 3개, 둘째 줄 2개). justify-items center, gap-2.
```jsx
// EmotionStickerGrid — 5종 감정 스티커 라디오그룹.
// 그리드: 1줄 3개 + 2줄 2개. PRD PART 3 와이어프레임 그대로.
// value: 현재 선택된 emotion key (or null). onChange(key) 호출.
import { EMOTIONS } from '../data/emotions';
import EmotionSticker from './EmotionSticker';

export default function EmotionStickerGrid({ value, onChange }) {
  return (
    <div role="radiogroup" aria-label="구매 시 감정 선택" className="grid grid-cols-3 gap-2 justify-items-center">
      {EMOTIONS.map((e) => (
        <EmotionSticker
          key={e.key}
          emoji={e.emoji}
          label={e.label}
          emotionKey={e.key}
          isSelected={value === e.key}
          isDimmed={value !== null && value !== e.key}
          onSelect={onChange}
        />
      ))}
    </div>
  );
}
```

**4. src/components/RecordTextField.jsx — 품목 입력**
controlled input, 한글 IME 안전(value/onChange만, onCompositionStart/End 추가 안 함 — React 19 controlled input 이 IME 처리). 라벨 위, input 아래.
```jsx
// RecordTextField — 품목 입력. 라벨 위 + 반투명 흰색 input.
// 한글 IME 입력 보존을 위해 단순 controlled input (React 가 처리).
export default function RecordTextField({ id, label, value, onChange, placeholder = '' }) {
  return (
    <label htmlFor={id} className="flex flex-col gap-1">
      <span className="text-body text-magic-cream">{label}</span>
      <input
        id={id}
        type="text"
        value={value}
        onChange={(e) => onChange(e.target.value)}
        placeholder={placeholder}
        className={[
          'min-h-[48px] w-full px-3 py-2',
          'rounded-card bg-white/15 border border-magic-gold/60',
          'text-body text-white placeholder-white/40',
          'focus:outline-none focus:border-magic-cyan focus:shadow-[0_0_12px_rgba(34,211,238,0.5)]',
          'transition-shadow',
        ].join(' ')}
      />
    </label>
  );
}
```

**5. src/components/RecordAmountField.jsx — 금액 입력 (콤마 포맷)**
내부 state 는 raw number (또는 0). 표시는 toLocaleString. 입력 처리: 사용자가 친 문자열에서 숫자가 아닌 모든 문자(콤마·공백·문자) 제거 → Number(parsed). NaN 또는 빈 문자열이면 0. 부모에는 number 만 전달.
```jsx
// RecordAmountField — 금액 숫자 입력 + 자동 콤마 포맷 + 우측 "원" 접미사.
// REC-01: 숫자만 허용. 표시는 콤마 (toLocaleString('ko-KR')). 부모 state 는 raw number.
// 빈 입력 = 0. 콤마/공백/한글 등은 입력 시 자동 제거 후 다시 포맷.
// inputMode="numeric" + pattern="[0-9,]*" 로 모바일 숫자 키패드 유도.

export default function RecordAmountField({ id, label, value, onChange }) {
  // value 는 number (0 이면 빈 표시).
  const display = value > 0 ? value.toLocaleString('ko-KR') : '';

  const handleChange = (e) => {
    const raw = e.target.value.replace(/[^0-9]/g, '');
    if (raw === '') {
      onChange(0);
      return;
    }
    const n = Number(raw);
    if (Number.isNaN(n)) {
      onChange(0);
      return;
    }
    onChange(n);
  };

  return (
    <label htmlFor={id} className="flex flex-col gap-1">
      <span className="text-body text-magic-cream">{label}</span>
      <div className="relative">
        <input
          id={id}
          type="text"
          inputMode="numeric"
          pattern="[0-9,]*"
          value={display}
          onChange={handleChange}
          className={[
            'min-h-[48px] w-full pl-3 pr-10 py-2',
            'rounded-card bg-white/15 border border-magic-gold/60',
            'text-body text-white placeholder-white/40',
            'focus:outline-none focus:border-magic-cyan focus:shadow-[0_0_12px_rgba(34,211,238,0.5)]',
            'transition-shadow',
          ].join(' ')}
        />
        <span className="absolute right-3 top-1/2 -translate-y-1/2 text-body text-magic-cream pointer-events-none">원</span>
      </div>
    </label>
  );
}
```

**6. src/components/BackButton.jsx — 좌상단 돌아가기**
≥48px 터치 타깃, 텍스트 + ← 화살표.
```jsx
// BackButton — 좌상단 ← 돌아가기. 클릭 시 onBack 호출.
export default function BackButton({ onBack }) {
  return (
    <button
      type="button"
      onClick={onBack}
      aria-label="뒤로 가기"
      className={[
        'inline-flex items-center gap-1',
        'min-h-[48px] px-3 py-2',
        'text-body text-magic-cream',
        'bg-transparent border-0 cursor-pointer',
        'focus-visible:outline-none focus-visible:shadow-focus-ring rounded-card',
      ].join(' ')}
    >
      <span aria-hidden="true">←</span>
      <span>돌아가기</span>
    </button>
  );
}
```

**7. src/components/HelperText.jsx — 비어있는 항목 안내문**
배열을 받아 한 줄씩 cream 색으로 표시. 빈 배열이면 null.
```jsx
// HelperText — 비활성 안내문 목록. messages: string[]. 빈 배열이면 렌더 안 함.
export default function HelperText({ messages }) {
  if (!messages || messages.length === 0) return null;
  return (
    <ul className="flex flex-col gap-1" role="status" aria-live="polite">
      {messages.map((m) => (
        <li key={m} className="text-label text-magic-cream/80">{m}</li>
      ))}
    </ul>
  );
}
```

**8. src/screens/RecordingScreen.jsx — 화면 3 합성**
state: item (string), amount (number, 0 = 빈), emotion (key | null).
검증: isValid = item.trim() !== '' && amount > 0 && emotion !== null.
helperMessages 도출: item.trim()==='' → '무엇을 샀는지 알려줘!' / amount<=0 → '얼마였는지 알려줘!' / emotion===null → '마음이 어땠는지 알려줘!'.
저장: PrimaryButton 의 onClick 에서 onSave({ item: item.trim(), amount, emotion, timestamp: new Date().toISOString() }) 호출.
돌아가기: BackButton onBack={onBack}.
스크롤: MobileFrame 안 콘텐츠 영역에 overflow-y:auto 적용 (5스티커가 안 들어갈 가능성 → MobileFrame 의 inner z-10 wrapper 도 overflow 허용).

```jsx
import { useMemo, useState } from 'react';
import MobileFrame from '../components/MobileFrame';
import SparkleBackground from '../components/SparkleBackground';
import ScreenTitle from '../components/ScreenTitle';
import PrimaryButton from '../components/PrimaryButton';
import BackButton from '../components/BackButton';
import RecordTextField from '../components/RecordTextField';
import RecordAmountField from '../components/RecordAmountField';
import EmotionStickerGrid from '../components/EmotionStickerGrid';
import HelperText from '../components/HelperText';

// RecordingScreen — 화면 3 (기록).
// REC-01~05 합성 + 검증.
// onSave(record): localStorage 저장은 부모(App)에서 담당. 여기는 record 객체만 만들어 전달.
// onBack(): 입력 폐기 후 화면 2 복귀.
export default function RecordingScreen({ onSave, onBack }) {
  const [item, setItem] = useState('');
  const [amount, setAmount] = useState(0);
  const [emotion, setEmotion] = useState(null);

  const helperMessages = useMemo(() => {
    const msgs = [];
    if (item.trim() === '') msgs.push('무엇을 샀는지 알려줘!');
    if (amount <= 0) msgs.push('얼마였는지 알려줘!');
    if (emotion === null) msgs.push('마음이 어땠는지 알려줘!');
    return msgs;
  }, [item, amount, emotion]);

  const isValid = helperMessages.length === 0;

  const handleSubmit = () => {
    if (!isValid) return;
    onSave({
      item: item.trim(),
      amount,
      emotion,
      timestamp: new Date().toISOString(),
    });
  };

  return (
    <MobileFrame>
      <SparkleBackground />
      <div className="relative z-10 flex flex-col min-h-[667px] max-h-[667px] overflow-y-auto px-4 py-4 gap-4">
        <div className="flex">
          <BackButton onBack={onBack} />
        </div>

        <div className="flex flex-col gap-1">
          <ScreenTitle>📖 오늘의 마법서</ScreenTitle>
          <p className="text-body text-magic-cream text-center">오늘 너의 모험을 적어볼까?</p>
        </div>

        <RecordTextField
          id="record-item"
          label="무엇을 샀어?"
          value={item}
          onChange={setItem}
        />

        <RecordAmountField
          id="record-amount"
          label="얼마였어?"
          value={amount}
          onChange={setAmount}
        />

        <div className="flex flex-col gap-2">
          <span className="text-body text-magic-cream">살 때 마음이 어땠어?</span>
          <EmotionStickerGrid value={emotion} onChange={setEmotion} />
        </div>

        <HelperText messages={helperMessages} />

        <div className="mt-auto pt-2">
          <PrimaryButton disabled={!isValid} onClick={handleSubmit}>
            마법서에 적기 ✍️
          </PrimaryButton>
        </div>
      </div>
    </MobileFrame>
  );
}
```

**9. src/components/MobileFrame.jsx — 스크롤 허용**
현재 inner 가 overflow-hidden 이라 RecordingScreen 의 overflow-y-auto 가 동작하려면 inner div 의 overflow 를 풀어야 한다. 단, 외곽 카드의 `overflow-hidden` (그라데이션 클립) 은 유지. 안쪽 z-10 wrapper 의 overflow-hidden 은 제거 (현재 코드에는 inner 에 overflow 없음 — 외곽 카드만 overflow-hidden, OK). **확인만 하고 수정 불필요한 경우엔 그대로 둘 것.** 외곽이 overflow-hidden 이면 RecordingScreen 안에서 overflow-y-auto 한 컨테이너가 카드 내부 스크롤을 만들어주므로 별도 수정 없이 동작한다. 실제로 확인 후 추가 변경 필요 시에만 수정.

**검증 체크리스트:**
- 모든 새 파일이 ASCII 줄바꿈 LF 로 작성됨
- 모든 import 경로가 상대 경로로 정확함
- PRD PART 3 카피가 글자 그대로 들어감 (😍🤔😅😢✨ + 한글 라벨)
- 5종 emotion key 가 PRD PART 5 명세 (need/considered/impulse/regret/satisfied) 와 정확히 일치
- 모든 인터랙티브 요소 min-h-[48px]
- 입력 미완성 시 PrimaryButton 의 aria-disabled 동작 (HTML disabled 속성 사용 금지)
  </action>
  <verify>
    <automated>cd /Users/gowonho/Downloads/20260510/project1 && grep -c "EMOTIONS" src/data/emotions.js && grep -c "role=\"radio\"" src/components/EmotionSticker.jsx && grep -c "toLocaleString" src/components/RecordAmountField.jsx && grep -c "마법서에 적기" src/screens/RecordingScreen.jsx && grep -c "무엇을 샀는지 알려줘" src/screens/RecordingScreen.jsx && grep -c "RecordingScreen" src/screens/RecordingScreen.jsx && grep -E "need|considered|impulse|regret|satisfied" src/data/emotions.js | wc -l | awk '{ if ($1 >= 5) print "OK: 5 emotions"; else { print "FAIL: only " $1 " emotions"; exit 1 } }'</automated>
  </verify>
  <done>
- src/data/emotions.js, EmotionSticker.jsx, EmotionStickerGrid.jsx, RecordTextField.jsx, RecordAmountField.jsx, BackButton.jsx, HelperText.jsx, RecordingScreen.jsx 8개 파일 존재
- 5종 emotion key (need/considered/impulse/regret/satisfied) 모두 정의
- RecordingScreen 이 onSave + onBack props 받고, 검증·헬퍼·CTA 합성 완료
- 아직 App.jsx 와는 결합 안 함 (그건 Task 2)
  </done>
</task>

<task type="auto">
  <name>Task 2: localStorage 영속화 (storage.js) + App.jsx 라우팅 결합 + 새로고침 검증</name>
  <files>
    src/lib/storage.js,
    src/App.jsx
  </files>
  <action>
records 의 localStorage 영속화 추상화를 만들고, App.jsx 의 'recording' 분기를 PlaceholderScreen 에서 RecordingScreen 으로 교체한다. PlaceholderScreen 자체는 삭제하지 않음(다음 작업에서 필요할 수 있음 — 단순 미사용).

**1. src/lib/storage.js — records 추상화**
```js
// storage — localStorage 영속화. records 는 배열 (빈 배열 = 기본값).
// JSON 파싱 실패·저장소 접근 실패 시 모두 안전 폴백 (빈 배열 / no-op).
// PLAT-03 (records 영속화) 의 단일 진입점.

const RECORDS_KEY = 'records';

export function loadRecords() {
  try {
    const raw = localStorage.getItem(RECORDS_KEY);
    if (!raw) return [];
    const parsed = JSON.parse(raw);
    return Array.isArray(parsed) ? parsed : [];
  } catch (e) {
    return [];
  }
}

export function appendRecord(record) {
  try {
    const current = loadRecords();
    const next = [...current, record];
    localStorage.setItem(RECORDS_KEY, JSON.stringify(next));
    return next;
  } catch (e) {
    // localStorage 접근 실패해도 흐름은 멈추지 않음 (시연 안전성)
    return [record];
  }
}
```

**2. src/App.jsx — recording 분기 교체**
- import 추가: RecordingScreen, appendRecord
- import 제거: PlaceholderScreen (사용 안 함)
- 'recording' 분기 변경:
  ```jsx
  if (screen === 'recording') {
    return (
      <RecordingScreen
        onSave={(record) => {
          appendRecord(record);
          setScreen('dashboard');
        }}
        onBack={() => setScreen('dashboard')}
      />
    );
  }
  ```
- 다른 분기·useEffect (selectedMagic 복원) 는 손대지 말 것.

전체 교체본:
```jsx
import { useEffect, useState } from 'react';
import { ATTRIBUTES } from './data/attributes';
import { appendRecord } from './lib/storage';
import OnboardingScreen from './screens/OnboardingScreen';
import DashboardScreen from './screens/DashboardScreen';
import RecordingScreen from './screens/RecordingScreen';

// App — 화면 라우팅 머신.
// state: 'onboarding' | 'dashboard' | 'recording'
// 마운트 시 localStorage.selectedMagic 이 ATTRIBUTES 에 존재하는 유효 키면 'dashboard' 로 진입.
// 'recording' 은 화면 3 — RecordingScreen. onSave 시 records 영속화 + 대시보드 복귀.

function readStoredMagic() {
  try {
    return localStorage.getItem('selectedMagic');
  } catch (e) {
    return null;
  }
}

function isValidMagic(key) {
  return !!key && ATTRIBUTES.some((a) => a.key === key);
}

export default function App() {
  const [screen, setScreen] = useState('onboarding');
  const [selectedKey, setSelectedKey] = useState(null);

  useEffect(() => {
    const stored = readStoredMagic();
    if (isValidMagic(stored)) {
      setSelectedKey(stored);
      setScreen('dashboard');
    }
  }, []);

  const goToOnboarding = () => {
    setSelectedKey(null);
    setScreen('onboarding');
  };

  if (screen === 'recording') {
    return (
      <RecordingScreen
        onSave={(record) => {
          appendRecord(record);
          setScreen('dashboard');
        }}
        onBack={() => setScreen('dashboard')}
      />
    );
  }
  if (screen === 'dashboard') {
    return (
      <DashboardScreen
        selectedKey={selectedKey}
        onRecord={() => setScreen('recording')}
        onBackToOnboarding={goToOnboarding}
      />
    );
  }
  return (
    <OnboardingScreen
      onComplete={(key) => {
        setSelectedKey(key);
        setScreen('dashboard');
      }}
    />
  );
}
```

**검증 체크리스트:**
- storage.js 의 try/catch 두 함수 모두 빈 배열/단일 record 폴백 갖춤
- App.jsx 에서 PlaceholderScreen import 가 제거됨 (사용처 없음)
- App.jsx 에서 RecordingScreen + appendRecord import 추가됨
- 'recording' 분기에서 onSave 가 appendRecord 호출 후 setScreen('dashboard')
- 검증: dev 서버(이미 5173 에서 돌아가는 중)에서 화면 3 입력 → 저장 → 대시보드 복귀 확인. 이후 새로고침 → records 가 localStorage 에 유지됨 확인 (DevTools Application 탭 또는 콘솔 `JSON.parse(localStorage.getItem('records'))`).
  </action>
  <verify>
    <automated>cd /Users/gowonho/Downloads/20260510/project1 && grep -c "loadRecords\|appendRecord" src/lib/storage.js && grep -c "RECORDS_KEY = 'records'" src/lib/storage.js && grep -c "RecordingScreen" src/App.jsx && grep -c "appendRecord" src/App.jsx && (grep -c "PlaceholderScreen" src/App.jsx | awk '{ if ($1 == 0) print "OK: PlaceholderScreen unused"; else { print "FAIL: PlaceholderScreen still imported"; exit 1 } }')</automated>
  </verify>
  <done>
- src/lib/storage.js 가 loadRecords·appendRecord 두 함수를 export 하고 try/catch 폴백 포함
- src/App.jsx 의 'recording' 분기가 RecordingScreen 으로 교체되고 onSave 가 appendRecord 호출
- App.jsx 에서 PlaceholderScreen import 가 제거됨
- 화면 3 → 저장 → 대시보드 복귀 → 새로고침 후 localStorage 'records' 배열 유지 (수동 dev 확인)
  </done>
</task>

<task type="auto">
  <name>Task 3: 프로덕션 빌드 검증 + vite.config base 설정 + DEPLOY.md 가이드 작성</name>
  <files>
    vite.config.js,
    .planning/quick/260510-h7y-phase-3-3-localstorage/DEPLOY.md
  </files>
  <action>
프로덕션 빌드가 깨끗이 통과하고, Netlify drop / Vercel CLI 두 옵션으로 사용자가 1~3분 안에 배포 URL 을 얻을 수 있게 한다. 자동 배포는 시도하지 않음 (인증 필요).

**1. vite.config.js — base: './' 추가**
정적 호스팅(Netlify drop, subpath, file://) 모두 안전하도록 상대 경로 base 추가.
```js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  // 정적 호스팅(Netlify drop · subpath · 파일 직접 열기) 호환을 위해 상대 base.
  base: './',
})
```

**2. 프로덕션 빌드 검증**
```bash
cd /Users/gowonho/Downloads/20260510/project1
npm run build
```
- 에러 없이 종료 확인 (exit 0).
- dist/ 산출물 확인:
  ```bash
  ls -la dist/
  ls -la dist/assets/
  cat dist/index.html | head -30
  ```
- index.html 안의 script/link 경로가 `./assets/...` (상대 경로) 인지 확인 — base: './' 가 잘 적용됐다는 증거.

**3. 프로덕션 번들 동작 확인 (선택, 1분)**
dev 서버는 5173 에서 이미 돌아가는 중. preview 는 4173 별도 포트에서 한 번 띄워보고 화면 1→2→3 흐름 확인 후 종료(Ctrl+C 또는 background 종료).
```bash
# Bash run_in_background=true 로 띄우고 화면 1→2→3 빠르게 확인 후 종료
npx vite preview --port 4173
```
이건 verify 자동화 명령에 포함시키지 않음 (background 프로세스 정리가 까다로움). build 가 통과하는 것까지만 verify 에서 확인.

**4. DEPLOY.md 가이드 작성**
파일: `.planning/quick/260510-h7y-phase-3-3-localstorage/DEPLOY.md`

내용:
```markdown
# 배포 가이드 — 경제 마법사 학교 v1

`npm run build` 가 통과해 `dist/` 폴더가 만들어진 상태에서 시연용 공개 URL 을 얻는 두 가지 방법.

## 사전 준비 (공통)

```bash
cd /Users/gowonho/Downloads/20260510/project1
npm run build
# dist/ 폴더 생성 확인
ls dist/
```

`dist/index.html` 과 `dist/assets/` 폴더가 있어야 한다. 모든 경로가 상대(`./assets/...`)로 들어가 있어 어디 호스팅에 올려도 동작.

---

## 옵션 A — Netlify Drop (가장 빠름, 인증 불필요, 1분)

**장점:** 가입·로그인 없음, dist 폴더 통째로 드롭. URL 즉시 발급.
**단점:** 7일짜리 무료 임시 URL. 영구 URL 원하면 무료 가입 후 사이트로 claim.

### 절차
1. 브라우저에서 https://app.netlify.com/drop 열기.
2. Finder 에서 `project1/dist` 폴더를 그대로 페이지에 드래그앤드롭.
3. 30초 안에 `https://random-name-XXXXX.netlify.app` 형태 URL 발급.
4. URL 클릭해 화면 1 → 2 → 3 동작 확인.
5. URL 을 복사해 STATE.md `Quick Tasks Completed` 행 마지막에 추가:
   ```
   | 260510-h7y | Phase 3 화면 3 + localStorage + 배포 | 2026-05-10 | <commit> | <directory> | URL: https://... |
   ```

### 영구 URL 로 만들기 (선택)
1. Netlify 무료 가입 (GitHub/이메일).
2. 발급된 사이트 페이지에서 "Site settings" → "Change site name" → 원하는 이름 (예: `economic-wizard-school.netlify.app`).

---

## 옵션 B — Vercel CLI (Git 연동·자동 재배포 필요할 때)

**장점:** 영구 URL, 추후 코드 수정 시 `vercel --prod` 한 줄로 재배포.
**단점:** 첫 사용 시 토큰 인증 필요 (브라우저 OAuth, 사용자가 직접).

### 절차
1. CLI 설치 (전역, 1회):
   ```bash
   npm install -g vercel
   ```
2. 프로젝트 루트에서:
   ```bash
   cd /Users/gowonho/Downloads/20260510/project1
   vercel login   # 첫 1회만 — 이메일 입력 → 메일함에서 매직링크 클릭
   vercel --prod  # 빌드는 Vercel 서버에서 vite build 자동 수행
   ```
3. 첫 실행 시 프롬프트:
   - "Set up and deploy?" → Y
   - "Which scope?" → 본인 계정 선택
   - "Link to existing project?" → N
   - "Project name?" → 엔터 (기본값 폴더명)
   - "Directory?" → 엔터 (기본값 `./`)
   - "Override settings?" → N (Vite 자동 감지)
4. 1~2분 후 `https://<project>.vercel.app` URL 출력. 복사 후 STATE.md 에 기록.

---

## 옵션 C (참고) — dist 폴더만 다른 정적 호스팅으로

`base: './'` 가 이미 vite.config.js 에 들어있어 dist 폴더는 어디든(Surge / Cloudflare Pages / GitHub Pages / 사내 정적 서버) 그대로 올라간다. 단순 SPA 가 아닌 단일 index.html 이라 SPA fallback 설정 불필요.

---

## 배포 후 체크리스트

- [ ] URL 을 시크릿 창에서 열어 캐시 없이 동작 확인
- [ ] 화면 1: 5속성 동전 → 선택 → "선택 완료" → 1초 메시지 → 화면 2 진입
- [ ] 화면 2: 캐릭터 카드 + "오늘의 소비 기록하기" 클릭
- [ ] 화면 3: 품목 + 금액(콤마 자동) + 감정 스티커 → "마법서에 적기" → 화면 2 복귀
- [ ] 새로고침: 화면 2 가 다시 뜨고, DevTools Application > localStorage 에 `selectedMagic` + `records` 둘 다 유지
- [ ] 모바일에서 URL 열기: 375px 폭에 맞춰 표시되고 터치 타깃 충분
```

**검증 체크리스트:**
- vite.config.js 에 `base: './'` 한 줄 추가됨
- `npm run build` exit 0
- dist/index.html 존재, dist/assets/ 폴더 존재
- dist/index.html 의 script/link 경로가 `./` 로 시작 (상대 경로)
- DEPLOY.md 가 위 두 옵션을 모두 담고 있고 STATE.md 기록 안내 포함
  </action>
  <verify>
    <automated>cd /Users/gowonho/Downloads/20260510/project1 && grep -c "base: './'" vite.config.js && rm -rf dist && npm run build 2>&1 | tail -5 && test -f dist/index.html && test -d dist/assets && (grep -E 'src="\./|href="\./' dist/index.html | wc -l | awk '{ if ($1 >= 1) print "OK: relative paths"; else { print "FAIL: no relative paths in dist/index.html"; exit 1 } }') && test -f .planning/quick/260510-h7y-phase-3-3-localstorage/DEPLOY.md && grep -c "Netlify Drop" .planning/quick/260510-h7y-phase-3-3-localstorage/DEPLOY.md && grep -c "vercel --prod" .planning/quick/260510-h7y-phase-3-3-localstorage/DEPLOY.md</automated>
  </verify>
  <done>
- vite.config.js 에 base: './' 추가
- npm run build 성공, dist/ 산출물 생성, 모든 자산 경로 상대(./)
- DEPLOY.md 가 옵션 A (Netlify Drop) + 옵션 B (Vercel CLI) + 사후 체크리스트 + STATE.md 기록 안내 포함
- 사용자가 가이드 따라 1~3분 안에 공개 URL 획득 가능한 상태
  </done>
</task>

</tasks>

<verification>
**전체 흐름 검증 (수동, 1회):**
1. `npm run dev` (5173) 또는 `npx vite preview --port 4173` (dist 빌드) 에서:
   - 화면 1 동전 선택 → 화면 2 진입
   - 화면 2 "오늘의 소비 기록하기" → 화면 3 진입
   - 화면 3 입력: 품목 "슬라임" / 금액 "3000" 입력 시 "3,000 원" 으로 표시
   - 감정 스티커 "😅 그냥 갖고 싶었어" 클릭 → 골드 테두리·1.1배·나머지 흐림
   - "마법서에 적기" 클릭 → 화면 2 복귀
   - DevTools Application > Local Storage 에서 `records` 키 확인 → `[{item, amount, emotion, timestamp}]`
2. 페이지 새로고침 → records·selectedMagic 모두 유지
3. 화면 3 진입 → "← 돌아가기" → 화면 2 복귀, 입력 폐기 (다시 진입 시 빈 상태)
4. 입력 미완성 시 "마법서에 적기" 버튼 비활성·아래에 어떤 항목이 비었는지 안내문 표시
5. dist 빌드 후 DEPLOY.md 옵션 A 따라 Netlify Drop 으로 시연 URL 확보 (사용자 본인 수행)

**자동 검증 (각 task verify 합산):**
- 8개 새 컴포넌트 파일 + storage.js + 5종 emotion + RecordingScreen 합성 (Task 1)
- App.jsx 라우팅 결합 + PlaceholderScreen import 제거 (Task 2)
- npm run build 통과 + dist 상대 경로 + DEPLOY.md 두 옵션 (Task 3)
</verification>

<success_criteria>
- ROADMAP Phase 3 success criteria 5개 모두 만족 (품목·금액·감정 입력 / 스티커 선택 강조 / 미입력 안내 / localStorage 저장 + 새로고침 유지 / 시연 URL 가이드)
- REQUIREMENTS REC-01~05, PLAT-03, PLAT-04 모두 만족
- 화면 1→2→3 전체 흐름이 dev 와 prod 빌드 모두에서 작동
- 디자인 토큰 100% 상속 (UI-SPEC 의 색·폰트·spacing·shadow 그대로)
- PRD PART 3 카피 한 자도 안 바뀜
- Netlify Drop 으로 시연 URL 확보까지 사용자 1~3분
</success_criteria>

<output>
After completion, create `.planning/quick/260510-h7y-phase-3-3-localstorage/260510-h7y-SUMMARY.md`.
</output>
